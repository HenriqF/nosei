
cliente: NOVO BD 
servidor: mande regras
cliente: regras
servidor: criado.



// letras -> 
// numero -> 
// refere -> 



## imaginando isso:

    [pessoa]
        letras -> nome
        numero -> idade
        refere -> dados

    [dados]
        cpf   -> texto
        idade -> numero

    PASTAS:
    bancodedados/
        rules.txt -> tem as regras pra interpretacao

        pessoa/
            data/
                data1.txt
                data2.txt

            auto_index
            empty_space

        dados/
            data/
                data1.txt
                data2.txt

            auto_index
            empty_space


inserir em pessoa:

letras -> variavel
numero -> variavel2


datafile_size: 0


total: 4

 0     4     8     12     17    (+ datafile_size)
[21   ][12  ][17  ][pedro][6767]
 4     4      4     5      4
              ^


total_block_size -current -total


operacao -> se for verdade, retorna a tabela


PARA FAZER:


usar_banco("banco")
carregar_tabela("tabela")
show_tabela_carregada("tabela")
        show_indexes_tabela_carregada("tabela", indexes)

inserir_banco("tabela", []string{
    fmt.Sprintf("pedrinho#%v", i),
    fmt.Sprintf("Eu gosto muito de %v", i+3),
    fmt.Sprintf("A#%v", i),
    fmt.Sprintf("%v", i),
    fmt.Sprintf("%v", i),
    fmt.Sprintf("%v", i+420),
})


op = preparar_operacao("nome == descricao")
processar_tabela_carregada("tabela", op)